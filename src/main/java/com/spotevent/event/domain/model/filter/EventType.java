package com.spotevent.event.domain.model.filter;

public enum EventType {
    BASIC,    // L'évènement de base
    PROJECT,  // L'évènement lié à un projet
    FORMATION // L'évènement lié à une formation
}
